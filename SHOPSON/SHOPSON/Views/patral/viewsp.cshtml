@model List<SHOPSON.Areas.ADMIN.Models.SANPHAM>

        
   <!--features_items-->
</div>
    <div class="category-tab">
        <!--category-tab-->
        <div class="col-sm-12">
            <ul class="nav nav-tabs">
                <li class="active"><a href="#tshirt" data-toggle="tab">Chống thấm</a></li>
                <li><a href="#blazers" data-toggle="tab">Sơn trang trí</a></li>
                <li><a href="#sunglass" data-toggle="tab">Lót Nội Thất</a></li>
                <li><a href="#kids" data-toggle="tab">Lót Ngoại Thất</a></li>
               
            </ul>
        </div>
        <div class="tab-content">
            <div class="tab-pane fade active in" id="tshirt">
                @foreach(var item in Model) {
                <div class="col-sm-3">
                    <div class="product-image-wrapper">
                        <div class="single-products">
                            <div class="productinfo text-center">
                                <img src="~/Areas/img/sanpham/@item.ANH" style="width:100px;height:130px" />
                                <h2>@item.GIA.Value.ToString("N0")đ</h2>
                                <p>@item.MOTA</p>
                                <form action="/giohang/Additem" method="post">
                                    <input name="masp" value="@item.MASP" style="display:none" />
                                    <input id='@item.MASP' min='1' name="soluong" type='text' value='1' style="width:30px ;display:none" />
                                    <input type="submit" value="Thêm vào giỏ Hàng" class="btn au-btn--submit" />
                                </form>
                            </div>

                        </div>
                    </div>
                </div>
                }
                
                
            </div>

            <div class="tab-pane fade" id="blazers">
                @foreach (var item in Model)
                {
                    <div class="col-sm-3">
                        <div class="product-image-wrapper">
                            <div class="single-products">
                                <div class="productinfo text-center">
                                    <img src="~/Areas/img/sanpham/@item.ANH" style="width:100px;height:130px" />
                                    <h2>@item.GIA.Value.ToString("N0")đ</h2>
                                    <p>@item.MOTA</p>
                                    <form action="/giohang/Additem" method="post">
                                        <input name="masp" value="@item.MASP" style="display:none" />
                                        <input id='@item.MASP' min='1' name="soluong" type='text' value='1' style="width:30px ;display:none" />
                                        <input type="submit" value="Thêm vào giỏ Hàng" class="btn au-btn--submit" />
                                    </form>
                                </div>

                            </div>
                        </div>
                        
                    </div>

                }
                </div>

            <div class="tab-pane fade" id="sunglass">
                @foreach (var item in Model)
                {
                    <div class="col-sm-3">
                        <div class="product-image-wrapper">
                            <div class="single-products">
                                <div class="productinfo text-center">
                                    <img src="~/Areas/img/sanpham/@item.ANH" style="width:100px;height:130px" />
                                    <h2>@item.GIA.Value.ToString("N0")đ</h2>
                                    <p>@item.MOTA</p>
                                    <form action="/giohang/Additem" method="post">
                                        <input name="masp" value="@item.MASP" style="display:none" />
                                        <input id='@item.MASP' min='1' name="soluong" type='text' value='1' style="width:30px ;display:none" />
                                        <input type="submit" value="Thêm vào giỏ Hàng" class="btn au-btn--submit" />
                                    </form>
                                </div>

                            </div>
                        </div>
                    </div>
                }

                </div>

            <div class="tab-pane fade" id="kids">
                @foreach (var item in Model)
                {
                    <div class="col-sm-3">
                        <div class="product-image-wrapper">
                            <div class="single-products">
                                <div class="productinfo text-center">
                                    <img src="~/Areas/img/sanpham/@item.ANH" style="width:100px;height:130px" />
                                    <h2>@item.GIA.Value.ToString("N0")đ đ</h2>
                                    <p>@item.MOTA</p>
                                    <form action="/giohang/Additem" method="post">
                                        <input name="masp" value="@item.MASP" style="display:none" />
                                        <input id='@item.MASP' min='1' name="soluong" type='text' value='1' style="width:30px ;display:none" />
                                        <input type="submit" value="Thêm vào giỏ Hàng" class="btn au-btn--submit" />
                                    </form>
                                </div>

                            </div>
                        </div>
                    </div>
                }
                </div>

            
    </div><!--/category-tab-->

</div>